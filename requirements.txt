# Python dependencies for the covariate-balance test
# (python/balance_test/balance-test-ses.ipynb)
#
# Versions captured from the analysis environment (Python 3.13.2).
# The notebook imports only pandas, numpy and scipy; it reads .xlsx via openpyxl.
# The one-way ICC(1) is a hand-coded function in the notebook (no extra package).

pandas==2.3.1
numpy==2.3.1
scipy==1.16.0
openpyxl==3.1.5
et_xmlfile==2.0.0    # openpyxl dependency
